type Query {
  countries: [Country!]!
  country(code: ID!): Country
}

type Country {
  code: ID!
  name: String!
  native: String!
  phone: String!
  capital: String!
  currency: String!
  emoji: String!
  emojiU: String!
  continent: Continent!
  languages: [Language!]!
}
